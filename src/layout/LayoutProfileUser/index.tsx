import { ProfileMain } from '../../components/ProfileMain';
import { DefaultTemplate } from '../../templates/Default';

function LayoutProfileUser() {
  return (
    <DefaultTemplate>
      <ProfileMain />
    </DefaultTemplate>
  );
}

export { LayoutProfileUser };
